# tape-six-playwright's postinstall downloads a browser; a corrupt download once failed
# `npm ci` in CI before any test ran. Install the browser by hand instead:
# `npm run browser:install`.
ignore-scripts=true
